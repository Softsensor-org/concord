# Analytics Utilities

Put registered analytics utilities here or point `utility-register.csv` at the
real utility path in your repo.

Before a utility supports recurring reports or decisions, add a row to:

```text
00-ops/utilities/utility-register.csv
```

Platform-write utilities require approval, dry-run command, live command,
side-effect statement, rollback or reversal notes, and last verified date.
