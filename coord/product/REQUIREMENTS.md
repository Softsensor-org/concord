# Product Requirements

Replace this stub with your project's requirements (URS / functional /
non-functional). The `coord-ui` `/urs` view renders this file. Keep requirement
IDs stable so tickets, plan-record requirement-closure, and `/traceability` can
reference them.
